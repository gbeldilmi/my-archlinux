#!/usr/bin/env zsh

function _run() {
  cryptsetup luksOpen /dev/sdc1 backup
  mount /dev/mapper/backup /backup
}

if [ "$EUID" -ne 0 ]
then
  echo "Please run as root"
  exit 1
else
  _run
  exit 0
fi
