#!/usr/bin/env bash

function _run() {
  mount.nfs gemini:/srv/zodiac /srv/zodiac
  exportfs -arv
}

if [ "$EUID" -ne 0 ]
then
  echo "Please run as root"
  exit 1
else
  _run
  exit 0
fi

unset -f _run
